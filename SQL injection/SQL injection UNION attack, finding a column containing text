Lab: SQL injection vulnerability allowing login bypass

📝 Descrizione

Questo laboratorio contiene una vulnerabilità di **SQL injection** nella funzione di login dell'applicazione.  
L'obiettivo è sfruttare questa vulnerabilità per **effettuare l'accesso come utente administrator**, senza conoscerne la password.

---

⚙️ Passaggi eseguiti

1. Ho aperto il lab e avviato **Burp Suite** con il proxy attivo.
2. Ho tentato un login con credenziali arbitrarie (es: `test:test`) per catturare la richiesta HTTP.
3. Ho inviato la richiesta di login a **Burp Repeater** per testare la vulnerabilità.
4. Ho modificato il parametro `username` nella richiesta, impostandolo su: administrator'-- e lasciato il campo `password` con un valore qualsiasi.
5. Ho inviato la richiesta modificata.
6. L’applicazione ha effettuato correttamente il login come **administrator**.

---

🧠 Ragionamento tecnico

La vulnerabilità risiede nel modo in cui viene costruita la query SQL sul backend, senza un’adeguata sanitizzazione dei dati di input.  
Probabilmente la query originale è qualcosa di simile a:

SELECT * FROM users WHERE username = '<input>' AND password = '<input>';

Inserendo administrator'-- nel campo username, la query diventa:

SELECT * FROM users WHERE username = 'administrator'--' AND password = '...';

-- è il commento in SQL, quindi il resto della query (inclusa la condizione sulla password) viene ignorato.

Di conseguenza, se l’utente administrator esiste nel database, la query restituirà una riga valida e l’autenticazione avrà successo.

✅ Soluzione finale
Modificare il parametro username nella richiesta HTTP di login come segue:

username=administrator'--&password=anything

Dopo l’invio della richiesta, si viene autenticati come administrator e il lab risulta completato con successo.
